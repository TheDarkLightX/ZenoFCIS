# SHA-256 validation gates

The SHA-256 layer is not complete until the exact branch head passes the full workspace CI and the dedicated provider matrix under Rust 1.97.1. The review surface must then be reduced to source, lockfile, policy documentation, and read-only workflows.
