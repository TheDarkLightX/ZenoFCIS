[package]
name = "zeno-fcis-external-consumer"
version = "0.0.0"
edition = "2024"
rust-version = "1.97"
publish = false

[workspace]

[dependencies]
zeno-fcis = {
    version = "=0.1.0",
    path = "../../crates/zeno-fcis",
    default-features = false,
    features = ["std", "bootstrap", "composed-program", "backend"]
}

[lints.rust]
unsafe_code = "forbid"
