name: fixed-domain-machines

on:
  pull_request:
  push:
    branches:
      - main
      - "agent/**"

permissions:
  contents: read

concurrency:
  group: fixed-domain-machines-${{ github.ref }}
  cancel-in-progress: true

jobs:
  executable-composition:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Checkout exact source
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false

      - name: Install pinned Rust
        run: |
          rustup toolchain install 1.97.1 --profile minimal --component clippy --component rustfmt
          rustup target add wasm32-unknown-unknown --toolchain 1.97.1

      - name: Check formatting
        run: cargo +1.97.1 fmt --all -- --check

      - name: Lint fixed domain machines
        run: cargo +1.97.1 clippy -p zeno-fcis-domain --all-targets --all-features --locked -- -D warnings

      - name: Test fixed matrix laws
        run: cargo +1.97.1 test -p zeno-fcis-domain --all-features --locked

      - name: Test documentation
        run: cargo +1.97.1 test -p zeno-fcis-domain --doc --all-features --locked

      - name: Check no_std domain and umbrella surfaces
        run: |
          cargo +1.97.1 check -p zeno-fcis-domain --no-default-features --locked --target wasm32-unknown-unknown
          cargo +1.97.1 check -p zeno-fcis --no-default-features --features domain-machines --locked --target wasm32-unknown-unknown

      - name: Check warning-free documentation
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo +1.97.1 doc -p zeno-fcis-domain --all-features --locked --no-deps
